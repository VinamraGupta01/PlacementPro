package com.placement;

import org.junit.jupiter.api.Test;

/**
 * Minimal smoke test placeholder.
 * Real Spring context tests would need a running MySQL, so we keep this trivial
 * so `mvn package` works on fresh machines without a DB.
 */
class PlacementApplicationTests {

    @Test
    void contextHolds() {
        // intentionally empty – verifies compile & test infra wiring
    }
}
